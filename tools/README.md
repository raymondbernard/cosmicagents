# Tools

## Copy a project template

Run this from the workspace root:

```bash
python tools/copy_project_template.py project-template my-new-project
```

This creates a new folder under projects/ using the template structure.
